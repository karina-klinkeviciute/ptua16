def add(x: int, y: int) -> int:
    return x + y


def sub(x: int, y: int) -> int:
    return x - y


def prod(x: int, y: int) -> int:
    return x * y


def div(x: int, y: int) -> int:
    return x // y


if __name__ == "__main__":
    print("this is an arithmetics library. To use it, you need to import it into your own code")
